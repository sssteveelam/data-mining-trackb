# Browser-visible API base path. Keep `/api/v1` when using the Vite proxy or
# the Docker nginx proxy; set an absolute URL for a separately hosted backend.
VITE_API_BASE_URL=/api/v1

# Used only by `npm run dev`'s Vite proxy.
VITE_DEV_BACKEND_URL=http://localhost:8000
